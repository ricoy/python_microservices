[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
dev = "python hello_world_service.py"
prod = "./start.sh"
test = "pytest"

[packages]
flask = "~=2.1"
python-dotenv = "~=0.20"
uwsgi = "~=2.0.20"

[dev-packages]
pytest = "*"

[requires]
python_version = "3.10"
